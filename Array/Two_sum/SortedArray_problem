# TwoSum for SortedArray

def twoSum(nums,target):
  l.r=0,len(nums)-1
  while l<r:
    cursum=nums[l]+nums[r]
    
    if (cursum>taget):
       r-=1
     elif (curSum<target):
       l+=1
     elif (curSum==Target)
        
        return[l+1,r+1]
        
  return[]
       
